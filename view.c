#include <sys/types.h>
#include <sys/wait.h>


int main (int argc, const char * argv[]) {
// primero ver si se recibio argumentos (o sea el buffer)
// dps hacer lectura a ver si recibio por pipe (si no recibio por args)
// imprimir para cada elemento del buffer los datos (md5 PID fileName)
// queda ver como hacer para que deje de esperar inputs en el buffer compartido (ver video) (semforos)
// preguntar si se crea desde aca el shared memory
//
}
